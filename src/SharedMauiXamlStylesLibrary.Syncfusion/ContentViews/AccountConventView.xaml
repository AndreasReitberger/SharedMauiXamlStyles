<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="AndreasReitberger.Shared.Syncfusion.ContentViews.AccountConventView"
    
    xmlns:core="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
    xmlns:shared="clr-namespace:AndreasReitberger.Shared;assembly=SharedMauiXamlStylesLibrary"
    >
    <ContentView.Resources>
        <shared:SharedConverters />
    </ContentView.Resources>
    
    <ContentView.ControlTemplate>
        <ControlTemplate>
            <Grid
                Style="{StaticResource Style.Core.Grid.ShellTitleView}"
                IsVisible="{Binding Source={RelativeSource TemplatedParent}, Path=IsVisible}"
                IsEnabled="{Binding Source={RelativeSource TemplatedParent}, Path=IsEnabled}"
                Background="{Binding Source={RelativeSource TemplatedParent}, Path=Background}"
                HeightRequest="{Binding Source={RelativeSource TemplatedParent}, Path=HeightRequest}"
                ColumnDefinitions="Auto,*,Auto"
                >
                <core:SfAvatarView
                    AvatarName="{Binding Source={RelativeSource TemplatedParent}, Path=UsernameText}"
                    ImageSource="{Binding Source={RelativeSource TemplatedParent}, Path=UserImage}"
                    Style="{StaticResource Style.Syncfusion.SfAvatarView.Initials}"
                    />
                <ContentPresenter
                    IsVisible="{Binding Source={RelativeSource TemplatedParent}, Path=IsSyncing, Converter={StaticResource BooleanReverseVisibilityConverter}}"
                    Grid.Column="1"
                    />
                <HorizontalStackLayout
                    Grid.Column="2"
                    IsVisible="{Binding Source={RelativeSource TemplatedParent}, Path=IsSyncing}"
                    >
                    <ActivityIndicator
                        Margin="2"
                        IsRunning="{Binding Source={RelativeSource TemplatedParent}, Path=IsSyncing}"
                        Style="{StaticResource Style.Core.ActivityIndicator.Default}"
                        />
                    <Label
                        Margin="4,2"
                        Text="{Binding Source={RelativeSource TemplatedParent}, Path=IsSyncingText}"
                        Style="{StaticResource Style.Core.Label.Small}"
                        VerticalTextAlignment="Center"
                        HorizontalTextAlignment="Start"
                        />
                </HorizontalStackLayout>
            </Grid>
        </ControlTemplate>
    </ContentView.ControlTemplate>
</ContentView>
